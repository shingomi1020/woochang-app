# woochang-app (Next.js + Tailwind)

- 레포 **루트**에 이 파일/폴더 구조 그대로 두세요. (서브폴더로 중첩 X)
- Vercel → Project → Settings → **Root Directory**가 이 폴더를 가리키는지 확인하세요.

## Start
```bash
npm install
npm run dev
# build
npm run build && npm start
```
